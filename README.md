# RICH HALL Minsk

Адаптивный одностраничный сайт банкетного пространства RICH HALL. Проект не требует сборки: разметка, стили и логика находятся в `index.html`.

## Локальный запуск

```bash
python3 -m http.server 4173
```

После запуска сайт доступен по адресу `http://localhost:4173`.

## Форма бронирования

Форма отправляет `FormData` методом `POST`. Адрес сервиса приёма заявок задаётся в атрибуте `data-form-endpoint`:

```html
<form data-booking-form data-form-endpoint="https://адрес-сервиса">
```

Уведомление об успехе появляется только после успешного ответа сервера. Если адрес не задан или сервис недоступен, посетителю показывается сообщение с контактным телефоном.

## Публикация

Сайт публикуется через GitHub Pages из корня ветки `main`.

Перед финальной публикацией нужно заменить временные изображения и отзывы материалами заказчика, а также указать endpoint формы.
